# NBA Analytics App

## Overview
This project is a backend service built in Python that pulls NBA data using the `nba_api` library and exposes simple REST API endpoints for working with NBA-related data.

